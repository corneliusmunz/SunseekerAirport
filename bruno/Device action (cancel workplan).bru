meta {
  name: Device action (cancel workplan)
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/iot_mower/wireless/device/action
  body: json
  auth: inherit
}

headers {
  Accept-Language: de
  Authorization: Bearer {{access_token}}
}

body:json {
  {
    "method": "action",
    "appId": {{user_id}},
    "deviceSn": "{{deviceSerialNumber}}",
    "cmd": "cancel_time_tactics",
    "id": "cancelTimeTactics"
  }
}
